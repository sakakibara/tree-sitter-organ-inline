{
  "name": "tree-sitter-organ-inline",
  "version": "0.1.0",
  "description": "Inline-content grammar for Org mode",
  "main": "bindings/node",
  "scripts": {
    "test": "tree-sitter test"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "tree-sitter-cli"
    ]
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.24.0"
  }
}
